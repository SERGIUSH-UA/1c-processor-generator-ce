// ====================================================================
// Обробники для НеправильныйПример
// ====================================================================
// ✅ Синтаксис BSL правильний (CheckModules пройде)
// ❌ Є семантичні помилки (CheckConfig виявить)
// ====================================================================

// ❌ ПОМИЛКА 1: Пустий обробник (CheckConfig виявить через -EmptyHandlers)
&НаКлиенте
Процедура ПриОткрытии(Отказ)
    // Порожній обробник - знижує продуктивність!
КонецПроцедуры

// ❌ ПОМИЛКА 2: Невикористовувана процедура (CheckConfig виявить через -UnreferenceProcedures)
// Ця процедура ніде не викликається
&НаСервере
Функция НевикористовуванаФункция()
    Возврат "Ця функція ніде не викликається";
КонецФункции

// ❌ ПОМИЛКА 3: Пустий обробник команди (CheckConfig виявить через -EmptyHandlers)
&НаКлиенте
Процедура ТестоваяКоманда(Команда)
    // Порожній обробник команди
КонецПроцедуры
